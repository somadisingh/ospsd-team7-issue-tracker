# Copy to `.env` and fill in values. Never commit `.env`.
# From repository root: `load_dotenv()` loads this file (see `issue_tracker_service` / Alembic).

# --- Trello (OAuth; required for the API service) ---
TRELLO_API_KEY=
TRELLO_API_SECRET=
TRELLO_CALLBACK_URL=http://localhost:8000/auth/callback

# --- Database (Render Postgres, or local Docker Postgres) ---
# Example local: postgresql+psycopg://app:dev@127.0.0.1:5432/issue_tracker
# On Render, use the value linked from the Postgres add-on (often exposed as `DATABASE_URL`).
DATABASE_URL=

# --- Process (Render / local) ---
# Render sets `PORT`; the web service start command should use it (e.g. `--port $PORT`).
# The app does not read `PORT` in code—only the process manager / uvicorn does.
# PORT=8000

# --- HW3: Anthropic Claude (AI integration) ---
ANTHROPIC_API_KEY=

# Optional tuning. Safe defaults will be used if unset.
CLAUDE_MODEL=claude-sonnet-4-5
AI_MAX_TOOL_HOPS=6
AI_MAX_TOKENS=1024
# Allow AI to perform write operations (create/update/delete) on Trello boards.
# Set to "true" in production if AI mutations are desired; defaults to false.
AI_ALLOW_MUTATIONS=false

# --- HW3: Chat backend selection (cross-vertical integration) ---
# One of: local | slack
# Slack backend uses Team 9's slack-client-impl through the chat_client_api ABC.
CHAT_BACKEND=local
# Required when CHAT_BACKEND=slack. Get a bot token from the Slack workspace admin.
# SLACK_BOT_TOKEN=

# --- HW3: OpenTelemetry (OTLP/HTTP; traces + HTTP metrics for dashboards) ---
# Local dev: leave unset or set OTEL_SDK_DISABLED=true to skip exporters (also used in tests/CI).
# OTEL_SDK_DISABLED=true
# OTEL_SERVICE_NAME=issue-tracker-service
# Optional resource labels (comma-separated); Grafana wizard may suggest these:
# OTEL_RESOURCE_ATTRIBUTES=service.namespace=...,deployment.environment=production
#
# Single OTLP base URL (traces at .../v1/traces, metrics at .../v1/metrics are derived in code), or set TRACES/METRICS endpoints explicitly:
# OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp-gateway-prod-REGION.grafana.net/otlp
# OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=https://example.com/v1/traces
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=https://example.com/v1/metrics
# Per OTel, header values can be URL-encoded (e.g. Basic%20...); the service decodes for Grafana:
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic%20...,Another-Header=value

# --- CORS (Next.js frontend) ---
# Comma-separated list of allowed origins. Include the Vercel URL here in production.
CORS_ALLOW_ORIGINS=http://localhost:3000

# --- E2E / adapter tests against a deployed service (`tests/e2e/`) ---
# These names match `tests/e2e/conftest.py`. `E2E_BASE_URL` is not read by the tests—use `SERVICE_BASE_URL`.
# `SERVICE_SESSION_TOKEN` comes from a real browser OAuth flow to the deployed app (or manual callback).
# SERVICE_BASE_URL=https://your-service.onrender.com
# SERVICE_SESSION_TOKEN=
# Trello-backed e2e (same as CircleCI, when running real API tests) also use:
# TRELLO_TOKEN=
# TRELLO_BOARD_ID=
